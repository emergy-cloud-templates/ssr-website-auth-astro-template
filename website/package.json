{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "prepare:aws": "rm -rf ssr_dist && mkdir -p ssr_dist/dist && cp -r dist/server ssr_dist/dist/ && cp ssr/lambda.js ssr_dist && cp ssr/package.json ssr_dist && cp ssr/test.js ssr_dist",
    "test:ssr": "npm run build && npm run prepare:aws && cp ssr/lambda-local.js ssr_dist && cd ssr_dist && node test.js"
  },
  "dependencies": {
    "@astrojs/node": "^9.5.2",
    "@astrojs/preact": "^4.1.3",
    "@iconify-json/heroicons": "^1.2.3",
    "@iconify-json/lucide": "^1.2.86",
    "@nanostores/preact": "^1.0.0",
    "@supabase/ssr": "^0.8.0",
    "@supabase/supabase-js": "^2.91.0",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.16.15",
    "astro-icon": "^1.1.5",
    "dotenv": "^17.2.3",
    "nanostores": "^1.1.0",
    "preact": "^10.28.2",
    "tailwindcss": "^4.1.18"
  }
}